# Quick Start Guide - MCP Integration

## Пошаговая инструкция для запуска

### Шаг 1: Проверка зависимостей ✅

Все зависимости уже добавлены в проект. Синхронизируйте Gradle:

```bash
./gradlew build
```

Или в Android Studio: `File` → `Sync Project with Gradle Files`

### Шаг 2: Настройка MCP сервера

#### Для Android Эмулятора:

URL уже настроен на `ws://10.0.2.2:8080/mcp` в `ChatViewModel.kt`

#### Для физического устройства:

1. Найдите IP вашего компьютера:
   ```bash
   # macOS/Linux
   ifconfig | grep "inet "

   # Windows
   ipconfig
   ```

2. Обновите URL в `ChatViewModel.kt:24-27`:
   ```kotlin
   private val mcpRepository = McpRepository(
       serverUrl = "ws://YOUR_IP:8080/mcp", // Замените YOUR_IP
       enableDebugLogs = true
   )
   ```

3. Убедитесь, что MCP сервер запущен и доступен по сети

### Шаг 3: Запуск приложения

1. Запустите MCP сервер на вашем компьютере
2. Запустите Android приложение
3. Откройте ChatScreen

### Шаг 4: Проверка подключения

В верхней части ChatScreen вы увидите индикатор статуса:

- 🟢 **"Todoist подключен"** - всё работает!
- 🔴 **"Todoist отключен"** - проверьте MCP сервер

### Шаг 5: Тестирование

Попробуйте следующие команды в чате:

```
Создай задачу: Купить молоко завтра
```

```
Покажи мои задачи
```

```
Какие у меня проекты?
```

## Что было добавлено в проект

### ✅ Новые файлы:

**Data Layer:**
- `data/mcp/McpClient.kt` - WebSocket клиент
- `data/mcp/McpRepository.kt` - Репозиторий для MCP
- `data/mcp/models/McpRequest.kt` - JSON-RPC модели запросов
- `data/mcp/models/McpResponse.kt` - JSON-RPC модели ответов
- `data/mcp/models/TodoistModels.kt` - Модели Todoist

**Utils:**
- `utils/McpCommandParser.kt` - Парсер команд

**UI Components:**
- `ui/components/TodoistCard.kt` - Карточка задачи
- `ui/components/McpStatusIndicator.kt` - Индикатор статуса
- `ui/components/McpActionButton.kt` - Кнопки действий

### ✅ Обновленные файлы:

**Configuration:**
- `build.gradle.kts` (root) - Добавлен плагин serialization
- `app/build.gradle.kts` - Добавлены зависимости

**Data Models:**
- `model/Message.kt` - Добавлены поля для Todoist действий

**ViewModels:**
- `viewmodel/ChatViewModel.kt` - Интегрирован MCP функционал

**UI:**
- `ui/screens/ChatScreen.kt` - Добавлены MCP компоненты

**System:**
- `utils/SystemPrompts.kt` - Обновлен с Todoist промптом

## Структура вызовов

```
User Input (ChatScreen)
    ↓
ChatViewModel.sendMessageWithMcp()
    ↓
McpCommandParser.parseActions()  ←  Парсинг команд
    ↓
ChatViewModel.executeTodoistAction()
    ↓
McpRepository.executeAction()
    ↓
McpClient.[method]()  ←  WebSocket запрос
    ↓
MCP Server (JSON-RPC)
    ↓
Todoist API
```

## Основные функции

### Автоматическое распознавание

Приложение распознает команды автоматически:

```kotlin
// Пользователь пишет:
"Напомни мне купить молоко завтра"

// Система автоматически:
1. Парсит текст → TodoistAction.CreateTask
2. Отправляет Claude для ответа
3. Если confidence >= 0.9, создает задачу автоматически
4. Показывает результат в чате
```

### Быстрые действия

UI кнопки для мгновенных действий:

- **Новая задача** - открывает поле с шаблоном "Создай задачу: "
- **Мои задачи** - показывает все активные задачи
- **Проекты** - показывает список проектов

### Статус подключения

Реал-тайм индикатор состояния WebSocket:

- `Connected` - работает
- `Connecting` - подключается
- `Disconnected` - отключен (автоматически переподключится)
- `Error` - ошибка (кнопка "Переподключить")

## Примеры использования

### Создание задачи с приоритетом

```
Создай срочную задачу: Отправить отчет до пятницы
```

Система создаст:
- content: "Отправить отчет"
- due_string: "friday"
- priority: 4 (HIGH из-за слова "срочную")

### Просмотр задач

```
Что у меня на сегодня?
```

Система:
1. Получит список задач
2. Отфильтрует по сегодняшней дате
3. Покажет в виде карточек

### Выполнение задачи

```
Отметь задачу [ID] как выполненную
```

Или нажмите кнопку "Выполнить" на карточке задачи

## Отладка

### Включение логов

Логи уже включены. Смотрите в Android Studio Logcat:

```
Фильтр: McpClient | McpRepository | ChatViewModel
```

### Проверка подключения

```kotlin
// В коде
if (viewModel.isMcpConnected()) {
    // MCP доступен
} else {
    // MCP недоступен
}
```

### Принудительное переподключение

Нажмите кнопку "Переподключить" в индикаторе статуса или:

```kotlin
viewModel.reconnectMcp()
```

## FAQ

**Q: Приложение не подключается к MCP серверу**

A: Проверьте:
1. MCP сервер запущен?
2. Правильный IP адрес?
3. Firewall не блокирует порт 8080?
4. Для эмулятора используете `10.0.2.2`?

---

**Q: Команды не распознаются**

A: `McpCommandParser` ищет ключевые слова. Используйте:
- "создай задачу"
- "напомни мне"
- "покажи задачи"
- "мои проекты"

---

**Q: Как отключить MCP?**

A: В `ChatScreen.kt` установите:
```kotlin
val mcpEnabled = remember { mutableStateOf(false) }
```

---

**Q: Приложение падает при отправке сообщения**

A: Проверьте logcat на ошибки сериализации. Убедитесь, что:
1. Все data классы помечены `@Serializable`
2. Gradle sync выполнен
3. KSP плагин активен

---

**Q: Задачи создаются, но не отображаются**

A: Проверьте:
1. `cachedTasks` в ChatViewModel обновляется?
2. Logcat показывает успешный ответ от MCP?
3. UI правильно подписан на `cachedTasks` LiveData?

## Дополнительная настройка

### Изменение WebSocket timeout

В `McpClient.kt:55-58`:

```kotlin
private val okHttpClient = OkHttpClient.Builder()
    .connectTimeout(10, TimeUnit.SECONDS)    // Увеличьте
    .readTimeout(30, TimeUnit.SECONDS)       // Увеличьте
    .writeTimeout(10, TimeUnit.SECONDS)
    .build()
```

### Изменение delay переподключения

В `McpClient.kt:32`:

```kotlin
private val reconnectDelay: Long = 3000L  // Измените значение
```

### Отключение debug логов

В `ChatViewModel.kt:26`:

```kotlin
private val mcpRepository = McpRepository(
    serverUrl = "ws://10.0.2.2:8080/mcp",
    enableDebugLogs = false  // Отключить логи
)
```

## Следующие шаги

1. ✅ Запустите приложение и проверьте подключение
2. ✅ Протестируйте создание задач
3. ✅ Протестируйте просмотр задач
4. 📝 Добавьте unit тесты для `McpCommandParser`
5. 📝 Добавьте UI тесты для Compose компонентов
6. 🔒 Настройте безопасное хранение токенов (если нужно)
7. 🎨 Кастомизируйте UI под ваш дизайн

## Полезные ссылки

- 📖 [Полная документация](./MCP_INTEGRATION_README.md)
- 🔧 [Model Context Protocol](https://modelcontextprotocol.io/)
- 📋 [Todoist API](https://developer.todoist.com/)

---

**Готово! Теперь ваше приложение интегрировано с Todoist через MCP! 🎉**