# Поддержка Markdown в ClaudeChat

## Описание

В приложении реализована поддержка Markdown разметки для сообщений Claude. Это позволяет отображать форматированный текст с заголовками, списками, жирным/курсивом и другими элементами.

## Где работает Markdown

Markdown автоматически включается для:
1. ✅ **Многоагентного режима** - все ответы Claude отображаются с Markdown
2. ✅ **Summary сообщений** - сжатая история отображается с форматированием
3. ✅ **Обычных сообщений** - когда установлен флаг `useMarkdown = true`

## Поддерживаемые элементы

### 1. Заголовки

```markdown
# Заголовок 1
## Заголовок 2
### Заголовок 3
#### Заголовок 4
```

### 2. Жирный и курсив

```markdown
**Жирный текст**
*Курсив*
***Жирный курсив***
```

### 3. Списки

**Маркированный список:**
```markdown
- Элемент 1
- Элемент 2
  - Подэлемент 2.1
  - Подэлемент 2.2
```

**Нумерованный список:**
```markdown
1. Первый пункт
2. Второй пункт
3. Третий пункт
```

### 4. Ссылки

```markdown
[Текст ссылки](https://example.com)
```

### 5. Код

**Inline код:**
```markdown
Используйте функцию `print()` для вывода
```

**Блок кода:**
````markdown
```python
def hello():
    print("Hello, World!")
```
````

### 6. Цитаты

```markdown
> Это цитата
> Продолжение цитаты
```

### 7. Горизонтальная линия

```markdown
---
```

## Настройки отображения

В `MessageBubble.kt` настроены следующие параметры:

```kotlin
MarkdownText(
    markdown = message.text,
    modifier = Modifier.fillMaxWidth(),
    color = MessageTextAssistant,           // Цвет текста
    style = MaterialTheme.typography.bodyMedium,  // Стиль шрифта
    isTextSelectable = true,                // Возможность выделения текста
    disableLinkMovementMethod = false       // Поддержка ссылок
)
```

## Примеры использования

### Пример 1: Структурированный ответ

**Запрос пользователя:**
```
Расскажи про Python
```

**Ответ Claude (в многоагентном режиме):**
```markdown
# Python

## Что это такое?
Python - это высокоуровневый язык программирования.

## Основные особенности:
- **Простой синтаксис**
- **Динамическая типизация**
- **Богатая стандартная библиотека**

## Пример кода:
` ``python
print("Hello, World!")
` ``

> Python - отличный выбор для начинающих!
```

### Пример 2: Summary с форматированием

При сжатии истории, summary автоматически отображается с Markdown:

```markdown
📊 История диалога сжата. Сэкономлено ~350 токенов.

**Резюме:**

Пользователь **Алексей** мечтает стать космонавтом. Обсуждали:

1. Требования к образованию
2. Физическая подготовка
3. Психологические качества
4. Работа на МКС

*Основная цель* - понять путь к профессии космонавта.
```

## Технические детали

### Библиотека

Используется `compose-markdowntext` версии 0.5.4:

```kotlin
implementation("com.github.jeziellago:compose-markdown:0.5.4")
```

### Условие отображения

```kotlin
if ((message.useMarkdown || message.isSummary) && !message.isUser) {
    MarkdownText(...)  // Отображение с Markdown
} else {
    Text(...)  // Обычный текст
}
```

### Цветовая схема

- **Обычные сообщения:** `MessageTextAssistant`
- **Summary сообщения:** `MaterialTheme.colorScheme.onTertiaryContainer`
- **Пользовательские сообщения:** `MessageTextUser` (без Markdown)

## Проблемы и решения

### Проблема: Заголовки не отображаются

**Решение:**
Убедитесь, что:
1. Флаг `useMarkdown = true` установлен для сообщения
2. Используется правильный синтаксис: `# Заголовок`, `## Заголовок 2` и т.д.
3. Между `#` и текстом есть пробел

### Проблема: `***` отображается как есть

**Пояснение:**
- `***текст***` - это жирный курсив в Markdown
- Для заголовков используйте `#`, `##`, `###`

**Правильно:**
```markdown
### Это заголовок третьего уровня
***Это жирный курсив***
```

**Неправильно:**
```markdown
*** Заголовок  (будет отображено как жирный курсив со звездочками)
```

### Проблема: Код блоки не отображаются

**Решение:**
Используйте тройные обратные кавычки:

````markdown
```
код здесь
```
````

С указанием языка:

````markdown
```python
def hello():
    print("Hello")
```
````

## Ограничения

1. ⚠️ **Таблицы** - не поддерживаются в текущей версии библиотеки
2. ⚠️ **Изображения** - inline изображения не отображаются
3. ⚠️ **HTML теги** - не поддерживаются
4. ⚠️ **LaTeX формулы** - не поддерживаются
5. ⚠️ **Чекбоксы** - `- [ ]` не работают как чекбоксы

## Будущие улучшения

### Краткосрочные:
1. Добавить поддержку таблиц
2. Настраиваемый стиль для заголовков разных уровней
3. Подсветка синтаксиса для кода

### Среднесрочные:
1. Переход на более современную Markdown библиотеку
2. Поддержка LaTeX формул
3. Темы оформления для кода

### Долгосрочные:
1. Интерактивные элементы (чекбоксы)
2. Вставка изображений
3. Экспорт в HTML/PDF с сохранением форматирования

## Тестирование Markdown

### Тестовый запрос

Отправьте в **многоагентном режиме**:

```
Создай структурированную инструкцию по изучению Python с использованием всех возможностей Markdown:
- Заголовки разных уровней
- Списки (маркированные и нумерованные)
- Жирный и курсивный текст
- Блоки кода
- Цитаты
- Ссылки
```

Claude ответит с полным форматированием, которое будет корректно отображено!

## Заключение

Markdown поддержка делает сообщения Claude:
- ✅ Более читаемыми
- ✅ Структурированными
- ✅ Профессиональными
- ✅ Удобными для технических инструкций

**Результат:** Богатое форматирование текста для лучшего восприятия информации!